// types.ts (veya uygun bir dosya)
export interface Character {
    id: number;
    name: string;
    // Diğer gerekli alanlar...
  }
  
  export interface Location {
    id: number;
    name: string;
    // Diğer gerekli alanlar...
  }
  
  export interface Episode {
    id: number;
    name: string;
    // Diğer gerekli alanlar...
  }
  